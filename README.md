# Arbot-but-js
Arbot is javascript

Requirements:
 *   npm install mssql
 * 
 * Adjust the connection string to match your environment. This example assumes a trusted
 * Windows connection to a SQL Server LocalDB/SQLEXPRESS instance.
 */
